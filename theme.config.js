/** @type {const} */
const themeColors = {
  // Paleta Espaçolaser - Manual de Marca 2024
  // Cores Clássicas (Pantone)
  primary: { light: '#003FC3', dark: '#417CFF' },      // Pantone 2728 C (principal) / 2727 C (claro)
  background: { light: '#FFFFFF', dark: '#001C65' },   // Branco / Pantone 3591 C (azul escuro)
  surface: { light: '#F5F7FA', dark: '#002080' },      // Cinza claro / Azul médio
  foreground: { light: '#001C65', dark: '#FFFFFF' },   // Pantone 3591 C / Branco
  muted: { light: '#5A6B7D', dark: '#99BBFF' },        // Cinza azulado / Azul claro
  border: { light: '#E0E6ED', dark: '#334488' },       // Cinza claro / Azul escuro
  accent: { light: '#417CFF', dark: '#99BBFF' },       // Pantone 2727 C / Azul claro
  success: { light: '#22C55E', dark: '#4ADE80' },      // Verde
  warning: { light: '#FF9012', dark: '#FFAB40' },      // Pantone 3588 C (laranja)
  error: { light: '#DF007E', dark: '#FF4DA6' },        // Pantone 219 C (magenta)
};

module.exports = { themeColors };
